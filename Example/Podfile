source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'Camira_Example', :exclusive => true do
  pod 'Camira', :path => '../'
end

target 'Camira_Tests', :exclusive => true do
  pod 'Camira', :path => '../'

  pod 'Quick', '~> 0.8.0'
  pod 'Nimble', '3.0.0'
  pod 'FBSnapshotTestCase'
  pod 'Nimble-Snapshots'
end
